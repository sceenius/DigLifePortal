// unused, not clear whch modules to use

import { select, selectAll } from "d3-selection";
import { format } from "d3-format";
import { hierachy } from "d3-hierarchy";

import {
  scaleLinear,
  scaleTime,
  scaleOrdinal,
  schemeCategory10
} from "d3-scale";

import { axisTop } from "d3-axis";

export default {
  select,
  selectAll,
  scaleLinear,
  scaleTime,
  scaleOrdinal,
  schemeCategory10,
  axisTop,
  format,
  hierachy
};
